# Source: generated.run_tests

# We asked ChatGPT and DeepSeek to test edge cases

statement ok
SELECT * FROM testdata.satellites WHERE gm IS NULL

statement ok
SELECT * FROM $planets WHERE mass = 0

statement error TypeError
SELECT id, name, radius / NULLIF(density, 0) FROM testdata.satellites WHERE density > 0

statement ok
SELECT id, name, mass / diameter FROM $planets WHERE diameter > 0

statement error AmbiguousIdentifierError
SELECT s.name, p.name FROM testdata.satellites AS s JOIN $planets AS p ON s.planetId = p.id WHERE s.radius > p.diameter

statement ok
SELECT planetId, COUNT(*) FROM testdata.satellites GROUP BY planetId HAVING COUNT(*) > 10

# crashed Opteryx (excluded): SELECT name, LENGTH(name) FROM testdata.satellites WHERE LENGTH(name) < 3
statement ok
SELECT id, name, magnitude FROM testdata.satellites WHERE magnitude < -10 OR magnitude > 10 ORDER BY magnitude DESC

statement ok
SELECT id, name FROM $planets WHERE meanTemperature BETWEEN -200 AND 500 ORDER BY meanTemperature DESC

statement ok
SELECT p.name, COUNT(s.id) FROM $planets AS p LEFT JOIN testdata.satellites AS s ON p.id = s.planetId GROUP BY p.name ORDER BY COUNT(s.id) DESC

statement ok
SELECT id, name FROM testdata.satellites WHERE name LIKE '%-%'

statement error TypeError
SELECT id, name, ROUND(gravity, 0) FROM $planets

statement error AmbiguousIdentifierError
SELECT p.name, p.mass, s.name FROM $planets AS p RIGHT JOIN testdata.satellites AS s ON p.id = s.planetId

statement ok
SELECT name, id FROM $planets WHERE name NOT LIKE '%e%'

statement ok
SELECT id, name, ABS(density) FROM testdata.satellites WHERE density < 0

statement ok
SELECT name, radius FROM testdata.satellites ORDER BY radius NULLS FIRST

statement ok
SELECT p.name, COUNT(s.id) FROM $planets AS p LEFT JOIN testdata.satellites AS s ON p.id = s.planetId WHERE s.id IS NULL GROUP BY p.name

statement error TypeError
SELECT id, name, CASE WHEN albedo > 0.5 THEN 'HIGH' WHEN albedo BETWEEN 0.2 AND 0.5 THEN 'MEDIUM' ELSE 'LOW' END AS reflectivity FROM testdata.satellites

statement ok
SELECT name, orbitalEccentricity FROM $planets WHERE orbitalEccentricity = 0

statement ok
SELECT name, gravity FROM $planets WHERE gravity BETWEEN 0 AND 0.1

statement error AmbiguousIdentifierError
SELECT s.id, s.name, p.name FROM testdata.satellites AS s LEFT JOIN $planets AS p ON s.planetId = p.id WHERE p.id IS NULL

statement ok
SELECT planetId, COUNT(*) FROM testdata.satellites GROUP BY planetId ORDER BY COUNT(*) ASC

statement ok
SELECT id, name, ROUND(escapeVelocity / rotationPeriod, 2) AS escape_rotation_ratio FROM $planets WHERE rotationPeriod > 0

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets AS p FULL JOIN testdata.satellites AS s ON p.id = s.planetId WHERE p.id IS NULL OR s.id IS NULL

statement error TypeError
SELECT name, id, CAST(radius AS DECIMAL(10,3)) FROM testdata.satellites

statement ok
SELECT id, name FROM testdata.satellites ORDER BY RANDOM()

statement ok
SELECT * FROM testdata.satellites WHERE planetId IS NULL

statement ok
SELECT COUNT(*) FROM $planets WHERE mass = 0 OR diameter = 0

statement error AmbiguousIdentifierError
SELECT s.id, s.name, p.name FROM testdata.satellites AS s LEFT JOIN $planets AS p ON s.planetId = p.id WHERE p.id IS NULL

statement ok
SELECT id, name FROM $planets WHERE mass > 0 AND diameter = 0

# crashed Opteryx (excluded): SELECT id, name, gm, radius FROM testdata.satellites WHERE gm / NULLIF(radius, 0) > 1e6
statement ok
SELECT id, name FROM $planets WHERE name LIKE 'JUPITER%' ORDER BY name DESC

statement ok
SELECT planetId, COUNT(*) FROM testdata.satellites GROUP BY planetId ORDER BY COUNT(*) DESC

statement error NotImplementedError
SELECT name, planetId FROM testdata.satellites WHERE name = UPPER(name)

statement error NotImplementedError
SELECT id, name FROM $planets WHERE gravity IS NOT NULL ORDER BY gravity DESC LIMIT 3

statement ok
SELECT id, name, ROUND(orbitalEccentricity, 5) FROM $planets WHERE orbitalEccentricity > 0

statement ok
SELECT id, name FROM testdata.satellites WHERE name NOT LIKE '%moon%'

statement ok
SELECT name, density FROM testdata.satellites WHERE density BETWEEN 1 AND 3 ORDER BY density DESC

# crashed Opteryx (excluded): SELECT name, LENGTH(name) FROM testdata.satellites ORDER BY LENGTH(name) DESC LIMIT 5
statement ok
SELECT p.name, COUNT(s.id) FROM $planets AS p LEFT JOIN testdata.satellites AS s ON p.id = s.planetId GROUP BY p.name HAVING COUNT(s.id) > 3

statement ok
SELECT name, albedo FROM testdata.satellites WHERE albedo < 0 OR albedo > 1

statement ok
SELECT id, name, orbitalPeriod FROM $planets WHERE orbitalPeriod > 365 ORDER BY orbitalPeriod ASC

statement ok
SELECT name FROM $planets WHERE gravity > 10 ORDER BY gravity DESC

statement error AmbiguousIdentifierError
SELECT s.name, p.name FROM testdata.satellites AS s JOIN $planets AS p ON s.planetId = p.id WHERE s.radius > p.diameter

statement error AmbiguousIdentifierError
SELECT p.name, p.mass, s.name FROM $planets AS p RIGHT JOIN testdata.satellites AS s ON p.id = s.planetId

# crashed Opteryx (excluded): SELECT id, name, COALESCE(density, 0) FROM testdata.satellites
statement ok
SELECT p.name, p.mass, p.density FROM $planets AS p WHERE p.mass > 0 ORDER BY p.density DESC LIMIT 5

statement ok
SELECT planetId, COUNT(*) FROM testdata.satellites GROUP BY planetId ORDER BY COUNT(*) ASC

statement ok
SELECT id, name, mass FROM $planets WHERE mass > 5 ORDER BY mass NULLS LAST

statement ok
SELECT name, id FROM testdata.satellites ORDER BY RANDOM()

statement ok
SELECT name, density FROM $planets WHERE density BETWEEN 1 AND 10 ORDER BY density DESC LIMIT 3

statement ok
SELECT name, magnitude FROM testdata.satellites WHERE magnitude BETWEEN -5 AND 5 ORDER BY magnitude

# crashed Opteryx (excluded): SELECT id, name FROM testdata.satellites WHERE name LIKE '_o%'

statement ok
SELECT p.name AS planet_name, s.name AS satellite_name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.density * s.density > 10000

statement error ValueError
SELECT * FROM testdata.satellites WHERE albedo IS NULL OR CAST(albedo AS INTEGER) = 0

statement ok
SELECT MAX(avg_gm) FROM (SELECT AVG(gm) AS avg_gm FROM testdata.satellites GROUP BY planetId) AS A

statement ok
SELECT planetId, COUNT(*) AS moon_count FROM testdata.satellites GROUP BY planetId HAVING moon_count > 10

# crashed Opteryx (excluded): SELECT * FROM testdata.satellites WHERE LENGTH(name) > 10 AND SUBSTRING(name, 1, 1) = 'M'
statement ok
SELECT planetId, SUM(CASE WHEN radius > 1000 THEN 1 ELSE 0 END) AS large_moons FROM testdata.satellites GROUP BY planetId

statement error AmbiguousIdentifierError
SELECT p.name, s.name, p.mass * s.gm AS combined_mass FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId

statement ok
SELECT * FROM testdata.satellites s JOIN (SELECT id FROM $planets WHERE surfacePressure IS NOT NULL) p ON s.planetId = p.id

statement ok
SELECT * FROM testdata.satellites WHERE gm BETWEEN 1000 AND 2000 AND radius * density > 5000

statement ok
SELECT s.planetId, p.avg_mass FROM testdata.satellites s JOIN (SELECT id, AVG(mass) AS avg_mass FROM $planets GROUP BY id) p ON s.planetId = p.id

statement ok
SELECT p.name || ' - ' || s.name AS planet_satellite FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId

statement error InvalidInternalStateError
SELECT * FROM $planets p JOIN (SELECT DISTINCT planetId FROM testdata.satellites WHERE albedo > 0.5) s ON p.id = s.planetId

statement ok
SELECT planetId, SUM(radius * density) AS total_mass_index FROM testdata.satellites GROUP BY planetId

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.mass / s.gm > 100

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.surfacePressure IS NULL OR s.albedo IS NULL

statement error AmbiguousIdentifierError
SELECT p.name, s.name, (p.mass * s.gm) / (p.diameter * s.radius) AS mass_radius_ratio FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId

statement ok
SELECT * FROM testdata.satellites s JOIN (SELECT id, CONCAT('%', name, '%') AS name_pattern FROM $planets) p ON s.name = p.name_pattern AND s.planetId = p.id

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId JOIN (SELECT AVG(mass) AS avg_mass FROM $planets) m ON p.mass = m.avg_mass

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId JOIN (SELECT MAX(mass) * 0.1 AS max_mass_scaled FROM $planets) m ON p.mass = m.max_mass_scaled

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.density + s.density = p.density - s.density

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.rotationPeriod + s.magnitude = p.lengthOfDay - s.albedo

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.distanceFromSun * s.gm = p.perihelion / s.radius

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.aphelion + s.density = p.orbitalPeriod - s.magnitude

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.orbitalVelocity * s.albedo = p.orbitalInclination / s.gm

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.orbitalEccentricity + s.radius = p.obliquityToOrbit - s.density

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.meanTemperature * s.magnitude = p.surfacePressure / s.albedo

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.numberOfMoons + s.gm = p.mass - s.radius

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.escapeVelocity + s.albedo = p.rotationPeriod - s.gm

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.lengthOfDay * s.radius = p.distanceFromSun / s.density

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.perihelion + s.magnitude = p.aphelion - s.albedo

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.orbitalPeriod * s.gm = p.orbitalVelocity / s.radius

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.orbitalInclination + s.density = p.orbitalEccentricity - s.magnitude

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.obliquityToOrbit * s.albedo = p.meanTemperature / s.gm

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.surfacePressure + s.radius = p.numberOfMoons - s.density

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.mass * s.magnitude = p.diameter / s.albedo

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.rotationPeriod * s.density = p.lengthOfDay / s.magnitude

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.distanceFromSun + s.albedo = p.perihelion - s.gm

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.aphelion * s.radius = p.orbitalPeriod / s.density

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.orbitalVelocity + s.magnitude = p.orbitalInclination - s.albedo

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.orbitalEccentricity * s.gm = p.obliquityToOrbit / s.radius

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.meanTemperature + s.density = p.surfacePressure - s.magnitude

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.numberOfMoons * s.albedo = p.mass / s.gm

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.diameter + s.radius = p.gravity::double - s.density

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.escapeVelocity * s.magnitude = p.rotationPeriod / s.albedo

statement error AmbiguousIdentifierError
SELECT p.name, s.name FROM $planets p JOIN testdata.satellites s ON p.id = s.planetId WHERE p.lengthOfDay + s.gm = p.distanceFromSun - s.radius
