# libskene distribution version — single source of truth.
#
# 0.x while the on-disk format v1 is DRAFT (skene/FORMAT.md): publishing a
# frozen format is a 1.0 event, not a packaging default. The FORMAT version
# (format.h kVersion) and this distribution version are separate axes.
__version__ = "0.1.5"
