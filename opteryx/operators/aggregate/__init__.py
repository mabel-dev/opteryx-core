"""
Aggregate function registry (helpers.py) and the native aggregate kernels
(_agg_kernels.hpp). The aggregate operators themselves are PhysicalSteps the
native compiler lowers; there is no aggregate operator class.
"""
