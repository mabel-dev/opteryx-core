{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // empty'); if printf '%s' \"$cmd\" | grep -qE '(^|[[:space:];&|(])git([[:space:]]|$)'; then printf '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"git is hard-blocked for the assistant in this repo (assistant destroyed uncommitted work twice via git). Run git commands yourself.\"}}'; fi"
          }
        ]
      }
    ]
  }
}
